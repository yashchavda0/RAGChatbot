Act as a professional AI emgineer and plan 

I want to create a Full fledges RAG system chatbot, where the response comes from document, links provided or directly through web search.

Here I want to use Agents calling mechanism for all this tasks like i will create agents for smaller tasks and when the user  query cames then we will be using the indent classification based on which agents calling will be decided, like if there are a chain of task to be perform then initially a plan generator agent will be called that will create me a full plan of the agents and task to be perform and which agent will perform which task, then a plan validator agent will verify the plan that is generated by the plan generator agent and once the plan is verified then that chain of tasks will be performed by the agents and the interactions between the agents will be maintained by agents themselves through a common mechanism if possible and feasible? then once the flow is executed the response for the user is ready to be served and given to the user in frontend.

RAG system should have ocr - use paddleocr from hugging face (PaddleOCRVL), use milvus for vectorDB, and use Gemini LLM, all the credentials for theme will be provided by me in env.

create individual service files for this services.

agents/tools will also be created for this if needed.

let mw know how will you approach for this project creation and if you have any query then let mw know, even if it is a simple query let me know about it i will answer.
But the system should be end to end working without any flaw, with logging, DB calling/ storing data in DB whenever needed, class based services, agents registration and added nodes and edges.