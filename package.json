{
  "name": "ragchatbot",
  "version": "1.0.0",
  "private": true,
  "description": "Root build orchestration for the RAG Chatbot platform (widget + frontend).",
  "scripts": {
    "build:widget": "cd widget-builder && npm install && npm run build && cd .. && node scripts/copy-widget.js",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:widget && npm run build:frontend",
    "build:all": "npm run build"
  }
}
