# Dev-only dependencies (installed on top of requirements.txt in development)
# Production builds only use requirements.txt

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0

# Debugging
debugpy>=1.8.0
ipython>=8.18.0

# Code quality
ruff>=0.1.0
