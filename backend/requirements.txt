# FastAPI and Web Server
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0

# Agent Orchestration
langgraph>=0.2.0

# LLM + Embeddings (Gemini API — zero local memory)
google-generativeai>=0.3.0
numpy>=1.24.0

# Local Embeddings (sentence-transformers, optional)
# Install torch CPU-only for lower RAM: pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.2.0

# BM25 Hybrid Search
rank-bm25>=0.2.2

# LLM Provider: Groq (optional, used when LLM_PROVIDER=groq)
groq>=0.5.0

# LLM Provider: Azure OpenAI (optional, used when LLM_PROVIDER=azure)
openai>=1.0.0

# Vector Database (Milvus)
pymilvus>=2.4.0

# Object Storage (MinIO)
minio>=7.2.0

# Database (PostgreSQL)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Cache (Redis)
redis>=5.0.0

# Task Queue (Celery) - Updated to 5.4.0 for Redis 5.x compatibility
celery[redis]==5.4.0
flower==2.0.1

# Azure Document Intelligence (cloud OCR — zero local memory)
azure-ai-formrecognizer>=3.3.0

# Web Search
tavily-python>=0.3.0

# Authentication
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Document Processing
pypdf>=4.0.0
python-docx>=0.8.11

# Security
passlib[bcrypt]>=1.7.4

# HTTP / Web Scraping
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
Brotli>=1.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
